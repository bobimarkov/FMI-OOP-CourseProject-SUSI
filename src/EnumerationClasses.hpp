#ifndef ENUMS_HPP
#define ENUMS_HPP

enum class Student_Status { 
    GRADUATED = 1, 
    SIGNED = 2, 
    INTERRUPTED = 3,
    UNKNOWN
};

enum class Type {
    OPTIONAL = 1, 
    COMPULSORY = 2,
    UNKNOWN
};
#endif